import java.awt.*;
import javax.swing.*;
import java.io.*;
/*
@author Ryan Phan
@version 1 - May 14, 2019
<p>
This class will thank the user for playing the game.
</p>
*/
public class Tutorial extends JPanel
{
    /*
    JLabel background = new JLabel(new ImageIcon(getClass().getResource("\\Pictures\\Tutorial.png")));
    SpringLayout layout = new SpringLayout();
    /**
     * This constructor will create a Goodbye object. It will initialize the custom fonts
     * and register them to the Graphics environment as well as initialize the layout of the panel.
     * It will also call the printTitle() method, the printText() method, and the printFooter() method.

    public Tutorial()
    {
        initPlayer();
        setLayout(layout);
        add(background);
        setVisible(true);
    }

    private void initPlayer(){
        JLabel label2 = new JLabel( new Player.ColorIcon(Color.BLUE, 40, 40) );
        label2.setSize( label2.getPreferredSize() );
        label2.setLocation(400, 400);
        layout.putConstraint(SpringLayout.HORIZONTAL_CENTER, label2, 0, SpringLayout.HORIZONTAL_CENTER, this);
        layout.putConstraint(SpringLayout.VERTICAL_CENTER, label2, 0, SpringLayout.VERTICAL_CENTER, this);
        add(label2);

        Player animation2 = new Player(label2, 24);
        animation2.addAction("A", -3,  0);
        animation2.addAction("D", 3,  0);
        animation2.addAction("W",    0, -3);
        animation2.addAction("S",  0,  3);

        revalidate();
    }
    */
}